const AppointmentListButton = () => {
    
    const handle = () => {

    }
    
    return (
        <div className="AppointmentListButton">
            <button onClick={handle}>Appointment List</button>
        </div>
    )
}

export default AppointmentListButton;
